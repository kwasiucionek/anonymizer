# --- Web UI ---
Django>=5.2,<6.0
django-q2>=1.7
gunicorn>=23.0

# --- Silnik anonimizacji (anonymizer.py) ---
transformers>=4.30.0
torch>=2.0.0
morfeusz2>=1.9
# Uwaga: katalog modelu musi zawierać label_config.json

# --- PostgreSQL (opcjonalnie; domyślnie SQLite) ---
# psycopg[binary]>=3.1
